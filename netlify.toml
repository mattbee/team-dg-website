[build]
  functions = "lambda"

[context.production]
  command = 'gulp && eleventy build'
  publish = '_site/'
  environment = { AUCTION_LIVE = "false" }

[context.staging]
  command = 'gulp && eleventy build'
  publish = '_site/'
  environment = { AUCTION_LIVE = "true" }
